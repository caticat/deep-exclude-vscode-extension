{
  "name": "deep-exclude",
  "displayName": "Deep Exclude",
  "description": "Unified file/folder exclusion management — sync glob patterns to Explorer, search, and file watcher simultaneously.",
  "version": "0.1.0",
  "publisher": "panjie039",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": ""
  },
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": ["Other"],
  "keywords": ["exclude", "explorer", "search", "filter", "hidden"],
  "activationEvents": ["onStartupFinished"],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "deepExclude.exclude",
        "title": "Exclude (Deep Exclude)",
        "category": "Deep Exclude"
      },
      {
        "command": "deepExclude.addPattern",
        "title": "Add Pattern",
        "category": "Deep Exclude",
        "icon": "$(add)"
      },
      {
        "command": "deepExclude.toggle",
        "title": "Toggle Rule",
        "category": "Deep Exclude",
        "icon": "$(eye)"
      },
      {
        "command": "deepExclude.remove",
        "title": "Remove Rule",
        "category": "Deep Exclude",
        "icon": "$(trash)"
      },
      {
        "command": "deepExclude.toggleAllOn",
        "title": "Enable All Rules",
        "category": "Deep Exclude",
        "icon": "$(check-all)"
      },
      {
        "command": "deepExclude.toggleAllOff",
        "title": "Disable All Rules",
        "category": "Deep Exclude",
        "icon": "$(close-all)"
      },
      {
        "command": "deepExclude.restoreDefaults",
        "title": "Restore Defaults",
        "category": "Deep Exclude",
        "icon": "$(discard)"
      },
      {
        "command": "deepExclude.openSettings",
        "title": "Open Settings",
        "category": "Deep Exclude",
        "icon": "$(settings-gear)"
      }
    ],
    "views": {
      "explorer": [
        {
          "id": "deepExclude.pane",
          "name": "Excluded Items",
          "icon": "$(eye-closed)",
          "contextualTitle": "Deep Exclude"
        }
      ]
    },
    "menus": {
      "explorer/context": [
        {
          "command": "deepExclude.exclude",
          "group": "7_modification@1"
        }
      ],
      "view/title": [
        {
          "command": "deepExclude.addPattern",
          "when": "view == deepExclude.pane",
          "group": "navigation@1"
        },
        {
          "command": "deepExclude.toggleAllOn",
          "when": "view == deepExclude.pane",
          "group": "navigation@2"
        },
        {
          "command": "deepExclude.toggleAllOff",
          "when": "view == deepExclude.pane",
          "group": "navigation@3"
        },
        {
          "command": "deepExclude.restoreDefaults",
          "when": "view == deepExclude.pane",
          "group": "navigation@4"
        },
        {
          "command": "deepExclude.openSettings",
          "when": "view == deepExclude.pane",
          "group": "navigation@5"
        }
      ],
      "view/item/context": [
        {
          "command": "deepExclude.toggle",
          "when": "view == deepExclude.pane",
          "group": "inline@1"
        },
        {
          "command": "deepExclude.remove",
          "when": "view == deepExclude.pane",
          "group": "inline@2"
        }
      ]
    },
    "configuration": {
      "title": "Deep Exclude",
      "properties": {
        "deepExclude.excludeList": {
          "type": "object",
          "default": {},
          "description": "Glob patterns managed by Deep Exclude. true = active, false = disabled.",
          "scope": "resource"
        },
        "deepExclude.syncToFilesExclude": {
          "type": "boolean",
          "default": true,
          "description": "Sync exclusion list to files.exclude (affects Explorer, Quick Open, Symbol Search).",
          "scope": "resource"
        },
        "deepExclude.syncToSearchExclude": {
          "type": "boolean",
          "default": true,
          "description": "Sync exclusion list to search.exclude (affects full-text search Ctrl+Shift+F).",
          "scope": "resource"
        },
        "deepExclude.syncToWatcherExclude": {
          "type": "boolean",
          "default": true,
          "description": "Sync exclusion list to files.watcherExclude (file system watcher, performance optimization).",
          "scope": "resource"
        },
        "deepExclude.respectGitignoreInExplorer": {
          "type": "boolean",
          "default": false,
          "description": "Hide .gitignore'd files from the Explorer (sets explorer.excludeGitIgnore at workspace level).",
          "scope": "resource"
        },
        "deepExclude.respectGitignoreInSearch": {
          "type": "boolean",
          "default": false,
          "description": "Exclude .gitignore'd files from full-text search (sets search.useIgnoreFiles at workspace level).",
          "scope": "resource"
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.js",
    "watch": "node esbuild.js --watch",
    "vscode:prepublish": "node esbuild.js --production",
    "package": "vsce package",
    "publish": "vsce publish"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/vscode": "^1.75.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0",
    "@vscode/vsce": "^2.24.0"
  }
}
